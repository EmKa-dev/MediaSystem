# Better exception-handling overall, but more specifically in downloading data.

# Find a way to detect duplicate devices and avoid showing duplicates.

# Find a way to detect and remove devices that are no longer available.

# Add a mechanism for reporting download progress in DownloadService and displaying it.

# Offer option to delete downloaded temp-files on app exit.
	|_Create an options view.
		|_Create options class
			|_register options to DI.

#Reimplement videoviewer along with browsing and a corresponding viewmodel.
	|_ Figure out a way to show cover-images for videos in the browser.